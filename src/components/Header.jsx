// Top navigation: holds branding and global actions.
import React from 'react';

const Header = () => (
  <header className="app-header">
    <h1>Flow Builder</h1>
  </header>
);

export default Header;
