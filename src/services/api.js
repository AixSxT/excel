// API layer placeholder: isolate network calls from UI and state logic.
export const fetchRunResult = async (payload) => {
  // TODO: replace with real HTTP client (fetch/axios) once endpoints exist.
  console.log('fetchRunResult called with', payload);
  return { data: null };
};

export default { fetchRunResult };
